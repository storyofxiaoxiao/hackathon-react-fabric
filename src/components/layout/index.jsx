// import PropTypes from 'prop-types';
import React from "react";
export default function Layout({ children }) {
  return (
    <div>
      {/* <Header></Header> */}
      {children}
      {/* <Footer></Footer> */}
    </div>
  );
}